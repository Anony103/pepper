import React from 'react';
// import { product } from '../../data';

const Products = () => {
  return (
    <div className="flex flex-wrap bg-white">
      {/* {product.map((item, index) => (
        <div
          key={index}
          className={`p-4 py-10 text-center flex flex-col items-center w-full sm:w-1/2 md:w-1/3 lg:w-1/4 text-${item.color}`}
          style={{backgroundColor:`${item.bg}`}}
        >
          <img src={item.img} alt="Dr Pepper" className="max-w-32 h-auto mb-6" />
          <h3 className="text-xl font-extrabold font-sans">{item.title}</h3>
        </div>
      ))} */}
    </div>
  );
};

export default Products;
